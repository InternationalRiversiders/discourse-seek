# 自适应卡片排列

桌面多列列表按卡片实际高度填充较短的一列，窄屏保留原有单列。使用 CSS Grid 和 ResizeObserver，不拆分/移动组件 DOM，不改变 API 排序、分页、键盘顺序或表单草稿。卡片内容展开、图片/字体加载、容器缩放及列表增删会重新计算占位；卸载时移除监听与占位样式。不支持观察器的浏览器回退普通网格。

课评仅对全部为评价的卡片列表启用；课程、教师及其他信息列表不变。觅电覆盖店铺列表、收藏及详情的同区域推荐店铺。
